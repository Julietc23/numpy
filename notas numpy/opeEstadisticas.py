import numpy as np

array = np.random.randn()

    #operaciones estadisticas:

np.mean(array)      #calcula la media
np.median(array)    #calcula la mediana
np.std(array)       #calcula la desviación estandard
np.var(array)       #calcula la varianza
array.min()       #calcula el valor minimo
array.max()       #calcula el valor maximo