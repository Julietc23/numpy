import numpy as np

array = np.random.rand()

        #funciones universales
np.sqrt(array)      #raiz cuadrada
np.exp(array)       #exponencial
np.log(array)       #logaritmo
np.sin(array)       #seno
np.cos(array)       #cos
np.tan(array)       #tan
